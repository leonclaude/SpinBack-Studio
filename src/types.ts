export type SpinbackResult = {
  plain: string;
  cheeky: string;
  psa: string;
  succulent: string;
  toneSignal: "green" | "yellow" | "red";
  toneReason: string;
};

export type ExampleClause = {
  label: string;
  text: string;
};
